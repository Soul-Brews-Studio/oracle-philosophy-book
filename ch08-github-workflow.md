# บทที่ 8: GitHub Workflow — nnn/gogogo Pattern

## ประเด็นสำคัญ

เมื่อมนุษย์กับ AI ทำงานร่วมกันในโปรเจกต์ซอฟต์แวร์ ส่วนสำคัญไม่ใช่แค่โค้ด แต่เป็น **workflow** — ขั้นตอนที่ชัดเจน ที่ทั้งสองฝ่ายตกลงร่วมกัน มันเหมือนเรื่องของการรักษาระบบให้สะอาด เพื่อสามารถ iterate และเรียนรู้ได้อย่างรวดเร็ว

ในบทนี้ ผมเล่าเรื่องการสร้าง **nnn/gogogo pattern** — workflow ที่ออกแบบมาให้ทำงานร่วมกันระหว่าง AI กับมนุษย์อย่างมีประสิทธิภาพ ผ่านการสาธิตการสร้าง Snake Game ในหลักสูตรอบรม SIIT

---

## nnn = สร้าง Plan Issue

**nnn** ทำหน้าที่เป็นจุดเริ่มต้น — ใช้คำสั่งเดียวเพื่อสร้าง issue ที่เป็นแผนการทำงาน

```bash
nnn "สร้างเกมงูใน 5 นาที"
```

สิ่งที่เกิดขึ้น:
1. **ถามผู้ใช้** ว่าต้องการอะไร — ชื่อเรื่อง ปัญหา วิธีแก้
2. **สร้าง issue** ในรูปแบบที่มีโครงสร้าง — overview, structure, acceptance criteria
3. **ตอบกลับด้วย issue number** ตัวอย่าง: *"Created issue #44"*

สาธารณ์ในการสร้าง Snake Game demo สำหรับ workshop:

```markdown
## Snake Game Demo

Goal: สร้างเกมงูใน 5 นาที

Structure:
demo/snake-game/
├── index.html
├── style.css
└── game.js

Theme: Nature 🌿
```

ประโยชน์ของ nnn:
- **ไม่ต้องเขียนรายละเอียดเอง** — AI ช่วยจัดระเบียบ
- **Issue เป็น reference point** — ทั้งหมดจะไปเสา issue number นี้
- **เตรียม branch ได้ทันที** — gh issue develop #44 → สร้าง branch โดยอัตโนมัติ

---

## gogogo = Execute Plan

**gogogo** คือการบอก AI "ไปทำตามแผนเลย" — ไม่ต้องถามอีก เพียงปฏิบัติตามขั้นตอนที่ชัดเจน

```bash
gogogo
```

ตามด้วย **ขั้นตอนสาม: branch → commit → push → PR**

### 1. Branch
```bash
gh issue develop #44
# ⮕ สร้าง branch: 44-snake-game-demo
# ⮕ checkout ลงมา
```

### 2. Commit
สร้างไฟล์ 3 อัน (HTML + CSS + JS) จากนั้น:
```bash
git add -A
git commit -m "feat: Snake Game Demo - Nature Theme 🌿"
```

### 3. Push
```bash
git push -u origin 44-snake-game-demo
```

### 4. PR (Pull Request)
```bash
gh pr create --title "feat: Snake Game Demo" --body "Closes #44"
```

**เรื่องสำคัญ**: ที่ปรากฏในการสาธารณ์ workshop ผมหยุดที่ step 2 (commit) และลืม step 3-4 (push + PR) ผู้เข้าร่วมแสดงความคิดเห็น: *"gh flow should branching and pr? why you wrong?"* — และเขาถูกต้อง!

นี่คือ **lesson learned** ที่สำคัญ: gogogo = **ต้องครบ workflow** ไม่ใช่หยุดกลางทาง

---

## gh flow: Branch → Commit → Push → PR

GitHub flow ไม่ได้มีที่ไหนมาจาก "ทำให้แล้วเสร็จ" — มันคือ **system ที่ออกแบบให้ code review, feedback, และ iteration** มี 4 ขั้นตอนแบบชัดเจน:

| Step | Command | Purpose | Example |
|------|---------|---------|---------|
| **Branch** | `gh issue develop` | สร้างพื้นที่ทำงานแยกจาก main | 44-snake-game-demo |
| **Commit** | `git commit -m "..."` | บันทึกการเปลี่ยนแปลง พร้อมข้อความ | feat: Snake Game |
| **Push** | `git push -u origin` | ส่งไป GitHub | อัพเดท branch ที่ server |
| **PR** | `gh pr create` | ขอการทบทวนและ merge | เปิด PR สำหรับ review |

### ว่าทำไมต้องเท่านี้?

1. **Branch** = ไม่พัง main — ทุกคนสามารถ develop แยกกัน
2. **Commit** = บันทึกประวัติ — ในอนาคต ถ้ามีปัญหา git blame บอกใครเขียน
3. **Push** = ทำให้เห็น — ทั้ง human reviewer กับ AI tools สามารถดูได้
4. **PR** = ขอ blessing — ก่อน merge ลงไป main ต้องมี sign-off

**อุปสรรค** ในการสาธารณ์: network timeout ทำให้ push ไม่ได้ นั่นคือเหตุผลที่ทำไม workflow ต้องมี error handling — ถ้า network ล้ม ผลงานในเครื่องยังปลอดภัย

---

## บทเรียน: Complete Workflow Matters

การทำ Human-AI co-creation ไม่ต่างจากการทำ Agile — **ขั้นตอน** สำคัญกว่า **ความเร็ว**

### ข้อมูลจริงจากการสาธารณ์:
- **ถาม**: "สร้างเกมงูสำหรับ workshop ได้ไหม?"
- **AI สร้าง**:
  - Issue #44 ✅
  - Branch ✅
  - Code + commit ✅
  - Push ❌ (network timeout)
  - PR ❌ (missing)
- **ผลลัพธ์**: ไม่ครบวงจร

### ค่าที่ได้จากการทำให้ครบ:
1. **อาร์ติแฟกต์ยังอยู่ที่ branch** — ไม่ได้หาย
2. **Reviewers สามารถดู** — ผ่าน PR
3. **CI/CD สามารถทำงาน** — ถ้ามีการ push
4. **Team รู้ว่าเกิดอะไรขึ้น** — ผ่าน commit message

### ว่ากรณีอื่นๆ?

ในทีมที่มี multiple AI agents, workflow ก็ยังสำคัญ:

```
Engineer A (Human): nnn "add login feature"
AI Agent 1 (Coder): gogogo → create code
AI Agent 2 (Security): Check for vulnerabilities
AI Agent 3 (Reviewer): Check code style
Human: Reviews PR, approves merge
```

ถ้าไม่มี workflow ชัดเจน ก็กลายเป็น chaos — ใครไปแก้ไฟล์ไหน ใครรับผิดชอบ review, ใครจะ merge?

---

## Nature Theme Snake Game — สรุป

ในการสาธารณ์ workshop นั้น เราไม่ได้แค่สร้างเกมงู เราแสดง **ทั้ง workflow และการ iterate บนฟีดแบ็ก**:

**ครั้งแรก**: Issue ยาว 100 บรรทัด → ผู้เข้าร่วม: "มึนจัด"
**Iteration**: ปรับเป็น 30 บรรทัด → ชัดเจนขึ้น

**ครั้งแรก**: สถาปัตยกรรม 1 ไฟล์ → ผู้เข้าร่วม: "ต้อง split HTML/CSS/JS"
**Iteration**: แยกเป็น 3 ไฟล์

**ครั้งแรก**: ที่สี (SIIT brand purple) → ผู้เข้าร่วม: "เลือก Nature theme ดีกว่า"
**Iteration**: สีเขียวอ่อน ชา บราวน์ — สีธรรมชาติ

ทั้งหมดนี้เป็นเพราะ workflow ชัดเจน (nnn → gogogo) ทำให้ทำการ iterate ได้อย่างรวดเร็ว ไม่มี overhead

---

## สรุป

**nnn/gogogo pattern** คือ:
- **nnn** = ตกลงแผนร่วมกัน (issue)
- **gogogo** = ทำตามแผน (branch → commit → push → PR)
- **gh flow** = ระบบการทำงานร่วมกัน (ไม่ใช่ shortcut)

บทเรียน: ในการทำงานร่วมกันระหว่าง Human-AI, **workflow complete** สำคัญกว่า **partial fast** — ดีกว่าหยุดกลางทาง

Snake Game นั่นไม่ได้เป็นโค้ด — มันเป็น **artifact ของ workflow ที่ถูกต้อง**

---

**กล่าวอ้างอิง**: Workshop Demo, SIIT, 2025-12-26
**ยืมตัวอักษร**: Complete workflows > partial shortcuts | Iterate fast on feedback loops
